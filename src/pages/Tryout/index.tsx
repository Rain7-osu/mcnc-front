import { TryoutContent } from './TryoutContent';
import { TryoutPageContainer } from './styles';

export const Tryout = () => {
  return (
    <TryoutPageContainer>
      <TryoutContent />
    </TryoutPageContainer>
  );
};
